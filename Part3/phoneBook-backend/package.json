{
  "name": "phonebook-backend",
  "version": "1.0.0",
  "description": "backend for notebook app",
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch index.js",
    "start": "node index.js",
    "build:ui": "rm -rf dist && cd ../../Part2/phonebook  && npm run build && cp -r dist ../../Part3/phoneBook-backend",
    "deploy": "echo 'Enter your commit message: ' && read msg && git add . && git commit -m \"$msg\" && git push",
    "deploy:full": "npm run build:ui && npm run deploy"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.8.0",
    "morgan": "^1.11.0"
  }
}
